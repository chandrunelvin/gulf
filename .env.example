SMTP_HOST=
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASS=
SMTP_FROM_EMAIL=
SMTP_FROM_NAME=

# Where contact-form submissions are delivered. Defaults to SMTP_FROM_EMAIL if unset.
CONTACT_TO_EMAIL=

PORT=5174
